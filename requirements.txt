# Minimal dependencies for Fordypningsemne-prosjekt
# Pin only core libs actually imported by the codebase.
# Use `pip install -r requirements.txt` (or conda create --name env --file requirements.txt for full freeze variant).

pyomo==6.9.4
numpy==1.26.4
matplotlib==3.9.2
pyyaml==6.0.1

# Optional (only if you use Gurobi; remove if not needed)
# gurobi==12.0.2  # Requires a valid Gurobi license; usually installed via conda channel gurobi

# If you later need reproducibility across all packages use:
#   pip freeze > requirements-full.txt
